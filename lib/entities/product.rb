# Business entity -- Product -- that encapsulates specific values related to the product

class Product

  # Initializes the Product by passing in the arguments
  #
  # @param *arg
  # @param two [String] error_message
  def initialize(*args)
  	# Instantiate the product and return an instance to the caller
  end

  # Persists the instance of a Product
  #
  # @return [boolean] -- true if successfully persisted, false if failure
  def save
  	# Persist the instance of a Product
  	true
  end
end